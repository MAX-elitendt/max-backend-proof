# max-backend-proof

Minimal Node + Express backend proof prepared for Render deployment.

## Local run

```bash
npm install
npm start
```

## Endpoints

- `/`
- `/health`

## Deployment target

Render via `render.yaml` from GitHub repo `MAX-elitendt/max-backend-proof`.
